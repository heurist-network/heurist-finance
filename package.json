{
    "name": "heurist-finance",
    "version": "0.10.4",
    "description": "Heurist Finance — agent-driven financial terminal for Heurist Mesh",
    "type": "module",
    "scripts": {
        "build": "node scripts/build.js",
        "version": "npm run build && git add terminal/dist/app.mjs",
        "test": "vitest run",
        "test:watch": "vitest",
        "test:update": "vitest run --update"
    },
    "dependencies": {
        "esbuild": "^0.27.4",
        "fast-xml-parser": "^5.5.9",
        "futu-sdk": "^0.2.6"
    },
    "devDependencies": {
        "vitest": "^3.0.0"
    }
}
